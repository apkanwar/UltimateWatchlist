import SwiftUI

// ResumeView has been removed along with the Resume tab.
// This placeholder exists temporarily to avoid build issues if any references remain.
struct ResumeView: View {
    var body: some View { EmptyView() }
}
